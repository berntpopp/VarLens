---
phase: 05-ui-shell-cases
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/src/App.vue
  - src/renderer/src/components/EmptyState.vue
  - src/renderer/src/components/AppSidebar.vue
autonomous: true

must_haves:
  truths:
    - "App displays collapsible left sidebar with main content area"
    - "Sidebar can be collapsed to rail view with toggle button"
    - "Main content shows welcome empty state when no case selected"
  artifacts:
    - path: "src/renderer/src/App.vue"
      provides: "App shell with v-navigation-drawer and v-main layout"
      contains: "v-navigation-drawer"
    - path: "src/renderer/src/components/EmptyState.vue"
      provides: "Welcome screen component"
      contains: "No Case Selected"
    - path: "src/renderer/src/components/AppSidebar.vue"
      provides: "Sidebar wrapper with toolbar and collapse toggle"
      contains: "rail"
  key_links:
    - from: "src/renderer/src/App.vue"
      to: "src/renderer/src/components/AppSidebar.vue"
      via: "component import"
      pattern: "import AppSidebar"
    - from: "src/renderer/src/App.vue"
      to: "src/renderer/src/components/EmptyState.vue"
      via: "conditional rendering"
      pattern: "v-if.*!selectedCaseId"
---

<objective>
Create the application shell layout with collapsible left sidebar and main content area.

Purpose: Establish the foundational layout structure that all subsequent UI phases will build upon. The sidebar will house the case list, and the main area will display variants or welcome state.

Output: Working app shell with collapsible sidebar (240px width, rail toggle), empty state component displayed in main area.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ui-shell-cases/05-CONTEXT.md
@.planning/phases/05-ui-shell-cases/05-RESEARCH.md
@src/renderer/src/App.vue
@src/renderer/src/plugins/vuetify.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create EmptyState and AppSidebar components</name>
  <files>
    src/renderer/src/components/EmptyState.vue
    src/renderer/src/components/AppSidebar.vue
  </files>
  <action>
Create two new Vue 3 components using Composition API with TypeScript:

**EmptyState.vue:**
- Centered container using v-container with fill-height
- Large icon (mdi-folder-open-outline, size 120, grey color)
- Heading: "No Case Selected" (text-h5)
- Subtext: "Import a variant file or select a case from the sidebar to view variants"
- Use Vuetify typography classes (text-grey-darken-1, text-grey)

**AppSidebar.vue:**
- Use v-navigation-drawer with:
  - `permanent` prop (always visible, adjusts v-main width)
  - `:rail="rail"` prop for collapse state
  - `:width="240"` for expanded width
- Prepend slot with v-toolbar (density="compact"):
  - Title "Cases" (visible when not in rail mode)
  - Toggle button with chevron icon (mdi-chevron-left when expanded, mdi-chevron-right when rail)
- Default slot for case list content (will be filled by Plan 02)
- Export `rail` ref via defineExpose for parent access if needed
- Use `ref(false)` for rail state (starts expanded)

Both components should use `<script setup lang="ts">` syntax.
  </action>
  <verify>
- Files exist at correct paths
- `npm run typecheck` passes
- Components have proper TypeScript typing
  </verify>
  <done>
EmptyState displays centered welcome message with icon.
AppSidebar renders collapsible drawer with rail toggle.
  </done>
</task>

<task type="auto">
  <name>Task 2: Transform App.vue to shell layout</name>
  <files>
    src/renderer/src/App.vue
  </files>
  <action>
Replace the placeholder App.vue content with the application shell layout:

1. Import both new components (AppSidebar, EmptyState)

2. Template structure:
```vue
<v-app>
  <AppSidebar>
    <!-- Case list slot placeholder - will be populated in Plan 02 -->
    <v-list density="compact">
      <v-list-item title="Cases will appear here" />
    </v-list>
  </AppSidebar>

  <v-main>
    <!-- Conditional: EmptyState when no case selected, variant table later -->
    <EmptyState v-if="!selectedCaseId" />
    <!-- Future: VariantTable when case selected -->
  </v-main>
</v-app>
```

3. Script setup:
- Import `ref` from vue
- Create `selectedCaseId = ref<number | null>(null)` for selection state
- This will be connected to CaseList events in Plan 02

4. Verify the drawer pushes v-main content (not overlays):
- The `permanent` prop on v-navigation-drawer ensures this
- v-main automatically adjusts its margin based on drawer state
  </action>
  <verify>
- `npm run dev` launches app with sidebar on left
- Click rail toggle collapses sidebar to narrow rail
- Main content shows EmptyState welcome message
- Main content area adjusts width when sidebar collapses
- `npm run typecheck` passes
  </verify>
  <done>
App shell layout functional with collapsible sidebar and empty state in main area.
selectedCaseId state ready for Plan 02 case selection wiring.
  </done>
</task>

</tasks>

<verification>
After completing all tasks:
1. `npm run typecheck` - zero errors
2. `npm run lint` - zero errors
3. `npm run dev` - app launches with:
   - Left sidebar visible (240px width)
   - "Cases" title in sidebar toolbar
   - Rail toggle button works (collapses to narrow rail)
   - Main content shows EmptyState with icon and text
   - Main area resizes when sidebar collapses
</verification>

<success_criteria>
- App shell displays with left sidebar and main content area
- Sidebar collapses to rail and expands with toggle button
- EmptyState component renders welcome message in main area
- All TypeScript types check cleanly
- Layout responds correctly to sidebar collapse (no overlap)
</success_criteria>

<output>
After completion, create `.planning/phases/05-ui-shell-cases/05-01-SUMMARY.md`
</output>
