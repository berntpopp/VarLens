---
phase: 05-ui-shell-cases
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/renderer/src/components/CaseList.vue
  - src/renderer/src/components/DeleteCaseDialog.vue
  - src/renderer/src/components/AppSnackbar.vue
  - src/renderer/src/composables/useContextMenu.ts
  - src/renderer/src/App.vue
  - src/renderer/src/components/AppSidebar.vue
autonomous: true

must_haves:
  truths:
    - "Case list displays all imported cases with name, variant count, and date"
    - "Clicking a case selects it and emits selection event"
    - "Right-click opens context menu with delete option"
    - "Delete shows confirmation dialog with case name and variant count"
    - "Successful delete shows snackbar notification"
    - "Empty list shows placeholder message"
  artifacts:
    - path: "src/renderer/src/components/CaseList.vue"
      provides: "Case list with IPC loading, selection, and context menu"
      contains: "window.api.cases.list"
    - path: "src/renderer/src/components/DeleteCaseDialog.vue"
      provides: "Confirmation dialog for case deletion"
      contains: "v-dialog"
    - path: "src/renderer/src/components/AppSnackbar.vue"
      provides: "Toast notification component"
      contains: "v-snackbar"
    - path: "src/renderer/src/composables/useContextMenu.ts"
      provides: "Context menu positioning composable"
      exports: ["useContextMenu"]
  key_links:
    - from: "src/renderer/src/components/CaseList.vue"
      to: "window.api.cases.list"
      via: "IPC call in onMounted"
      pattern: "window\\.api\\.cases\\.list"
    - from: "src/renderer/src/components/CaseList.vue"
      to: "window.api.cases.delete"
      via: "IPC call on confirm"
      pattern: "window\\.api\\.cases\\.delete"
    - from: "src/renderer/src/App.vue"
      to: "src/renderer/src/components/CaseList.vue"
      via: "case-selected event"
      pattern: "@case-selected"
---

<objective>
Implement case list functionality with selection, context menu delete, confirmation dialog, and snackbar notifications.

Purpose: Complete the case management UI allowing users to view all imported cases, select one for viewing, and delete cases with proper confirmation flow.

Output: Fully functional case sidebar with selection state propagating to App.vue, right-click delete with confirmation, and feedback notifications.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ui-shell-cases/05-CONTEXT.md
@.planning/phases/05-ui-shell-cases/05-RESEARCH.md
@.planning/phases/05-ui-shell-cases/05-01-SUMMARY.md
@src/renderer/src/App.vue
@src/renderer/src/components/AppSidebar.vue
@src/shared/types/api.ts
@src/main/database/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create useContextMenu composable and supporting components</name>
  <files>
    src/renderer/src/composables/useContextMenu.ts
    src/renderer/src/components/DeleteCaseDialog.vue
    src/renderer/src/components/AppSnackbar.vue
  </files>
  <action>
Create three files for context menu infrastructure and feedback:

**useContextMenu.ts:**
Composable for positioning context menus at click coordinates:
```typescript
import { ref } from 'vue'

export function useContextMenu() {
  const show = ref(false)
  const x = ref(0)
  const y = ref(0)

  const open = (event: MouseEvent) => {
    x.value = event.clientX
    y.value = event.clientY
    show.value = true
  }

  const close = () => {
    show.value = false
  }

  return { show, x, y, open, close }
}
```

**DeleteCaseDialog.vue:**
Confirmation dialog component with promise-based API:
- Props: none (controlled via expose)
- Internal state: dialog ref, caseName ref, variantCount ref, resolvePromise ref
- `show(name: string, count: number): Promise<boolean>` method via defineExpose
- Template with v-dialog (max-width="400"):
  - Title: "Delete Case?"
  - Text: "Delete '{caseName}'? This will remove all {variantCount.toLocaleString()} variants."
  - Actions: Cancel button (calls cancel), Delete button (color="error", calls confirm)
- confirm() sets dialog false, resolves true
- cancel() sets dialog false, resolves false

**AppSnackbar.vue:**
Global snackbar notification component:
- Internal state: snackbar ref (boolean), message ref, color ref
- `show(msg: string, type: 'success' | 'error' = 'success')` method via defineExpose
- Template with v-snackbar:
  - v-model="snackbar"
  - :color="color"
  - :timeout="3000"
  - location="bottom right"
  - Actions slot with Close button
  </action>
  <verify>
- Files exist at correct paths
- `npm run typecheck` passes
- useContextMenu exports function
- Dialog and Snackbar expose show() methods
  </verify>
  <done>
Context menu composable ready for positioning.
DeleteCaseDialog returns promise on user choice.
AppSnackbar displays toast notifications.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create CaseList component with full functionality</name>
  <files>
    src/renderer/src/components/CaseList.vue
  </files>
  <action>
Create CaseList.vue with complete case management functionality:

**Props and Emits:**
```typescript
const emit = defineEmits<{
  'case-selected': [caseId: number]
  'case-deleted': [caseId: number]
}>()
```

**Template structure:**
1. Search text field (v-text-field):
   - v-model="search"
   - prepend-inner-icon="mdi-magnify"
   - placeholder="Search cases..."
   - density="compact"
   - hide-details
   - clearable
   - class="mx-2 mt-2"

2. v-list with selection:
   - v-model:selected="selected"
   - density="compact"
   - select-strategy="single-leaf"

3. Empty state for no cases:
   - v-list-item when filteredCases is empty
   - Show "No cases imported" or "No matching cases" based on search

4. v-list-item for each case:
   - v-for="caseItem in filteredCases" :key="caseItem.id"
   - :value="caseItem.id"
   - :title="caseItem.name"
   - :subtitle with variant count + formatted date
   - prepend slot with v-icon (mdi-dna)
   - active-color="primary"
   - @contextmenu.prevent="handleContextMenu($event, caseItem)"

5. Context menu (v-menu):
   - v-model="contextMenu.show.value"
   - :style="{ position: 'fixed', left: contextMenu.x.value + 'px', top: contextMenu.y.value + 'px' }"
   - location-strategy="static"
   - v-list inside with delete item (mdi-delete icon)

6. DeleteCaseDialog ref
7. AppSnackbar ref

**Script logic:**
- Import Case type from shared types (use type-only import)
- cases ref<Case[]>([])
- loading ref(false)
- search ref('')
- selected ref<number[]>([])
- contextMenuCase ref<Case | null>(null)
- Use useContextMenu composable
- Refs for dialog and snackbar components

- loadCases async function:
  - loading = true
  - cases = await window.api.cases.list()
  - loading = false

- filteredCases computed:
  - If no search, return cases sorted by created_at DESC
  - Filter by name.toLowerCase().includes(search.toLowerCase())

- formatDate function:
  - Use Intl.DateTimeFormat for "Jan 26" style

- watch(selected):
  - If selected[0] exists, emit('case-selected', selected[0])

- handleContextMenu(event, caseItem):
  - contextMenuCase = caseItem
  - contextMenu.open(event)

- handleDelete async:
  - Close context menu
  - const confirmed = await dialogRef.value?.show(case.name, case.variant_count)
  - If confirmed:
    - await window.api.cases.delete(case.id)
    - emit('case-deleted', case.id)
    - If deleted case was selected, clear selected
    - snackbarRef.value?.show(`Deleted "${case.name}"`)
    - Reload cases list

- onMounted: loadCases()
  </action>
  <verify>
- `npm run typecheck` passes
- Component handles empty cases array
- Component handles search filtering
- Context menu opens on right-click
- Delete flow calls IPC and shows snackbar
  </verify>
  <done>
CaseList loads cases from IPC, displays with selection, handles delete with confirmation and notification.
  </done>
</task>

<task type="auto">
  <name>Task 3: Integrate CaseList into App shell</name>
  <files>
    src/renderer/src/App.vue
    src/renderer/src/components/AppSidebar.vue
  </files>
  <action>
Wire CaseList into the app shell:

**AppSidebar.vue updates:**
- Add default slot for content
- Template should have slot after toolbar for case list content

**App.vue updates:**
1. Import CaseList component
2. Place CaseList inside AppSidebar slot:
```vue
<AppSidebar>
  <CaseList
    @case-selected="handleCaseSelected"
    @case-deleted="handleCaseDeleted"
  />
</AppSidebar>
```

3. Add event handlers:
```typescript
const handleCaseSelected = (caseId: number) => {
  selectedCaseId.value = caseId
}

const handleCaseDeleted = (caseId: number) => {
  // If deleted case was selected, clear selection
  if (selectedCaseId.value === caseId) {
    selectedCaseId.value = null
  }
}
```

4. Update main content conditional:
- EmptyState shows when selectedCaseId is null
- Add placeholder comment for future VariantTable when case selected

5. Ensure all imports are correct and TypeScript is happy
  </action>
  <verify>
- `npm run typecheck` passes
- `npm run lint` passes
- `npm run dev` shows:
  - Empty case list (unless test data imported)
  - Search field in sidebar
  - Selecting a case updates selectedCaseId
  - Main content changes from EmptyState to placeholder when case selected
  </verify>
  <done>
CaseList integrated into App shell.
Selection state flows from CaseList to App.vue.
Delete events clear selection when needed.
  </done>
</task>

</tasks>

<verification>
After completing all tasks:
1. `npm run typecheck` - zero errors
2. `npm run lint` - zero errors
3. `npm run dev` - app launches with full case management:
   - Sidebar shows case list (empty if no imports)
   - Search field filters cases
   - Click case selects it (highlighted, main content updates)
   - Right-click case opens context menu
   - Delete shows confirmation dialog
   - Confirmed delete removes case and shows snackbar
   - After delete, empty state shows if no cases remain
</verification>

<success_criteria>
- Case list displays all imported cases with name, variant count, and formatted date
- Clicking a case selects it and updates the main content area (shows placeholder instead of EmptyState)
- Right-click context menu appears at cursor position with delete option
- Delete confirmation dialog shows case name and variant count
- Confirmed delete calls IPC, refreshes list, shows success snackbar
- If deleted case was selected, selection clears and EmptyState shows
- Search filter narrows case list by name
- Empty state message when no cases exist
</success_criteria>

<output>
After completion, create `.planning/phases/05-ui-shell-cases/05-02-SUMMARY.md`
</output>
