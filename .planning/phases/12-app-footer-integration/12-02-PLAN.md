---
phase: 12-app-footer-integration
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - src/renderer/src/components/AppFooter.vue
  - src/renderer/src/App.vue
autonomous: false

must_haves:
  truths:
    - "User sees a persistent footer bar with #E5AA94 warm background at the bottom of every view"
    - "User can click a version element in the footer to see a popup menu with app and Electron version"
    - "User can click GitHub and license icon buttons to open external links in system browser"
    - "User sees a disclaimer status icon that reflects acknowledgment state (shield-check vs shield-alert)"
    - "User can click FAQ button in footer to trigger FAQ dialog"
    - "User sees a log viewer toggle button with error count badge that shows 0+ errors reactively"
    - "Temporary FAB button and demo log seeding are removed from App.vue"
  artifacts:
    - path: "src/renderer/src/components/AppFooter.vue"
      provides: "Persistent footer bar with version menu, external links, disclaimer/FAQ/log controls"
      min_lines: 80
    - path: "src/renderer/src/App.vue"
      provides: "App shell integrating AppFooter, removing temporary FAB"
      contains: "AppFooter"
  key_links:
    - from: "src/renderer/src/components/AppFooter.vue"
      to: "src/renderer/src/stores/logStore.ts"
      via: "storeToRefs for reactive error count"
      pattern: "storeToRefs.*useLogStore"
    - from: "src/renderer/src/components/AppFooter.vue"
      to: "src/preload/index.ts"
      via: "window.api.shell.openExternal for GitHub/license links"
      pattern: "window\\.api\\.shell\\.openExternal"
    - from: "src/renderer/src/components/AppFooter.vue"
      to: "src/preload/index.ts"
      via: "window.api.system.getVersion for version display"
      pattern: "window\\.api\\.system\\.getVersion"
    - from: "src/renderer/src/App.vue"
      to: "src/renderer/src/components/AppFooter.vue"
      via: "component import and event handlers"
      pattern: "AppFooter"
---

<objective>
Create the AppFooter component and integrate it into App.vue, replacing the temporary FAB and wiring all footer buttons to their respective subsystems (version info, external links, disclaimer, FAQ, log viewer).

Purpose: This is the user-facing deliverable of Phase 12 -- a persistent footer bar that gives users one-click access to version info, GitHub/license links, disclaimer status, FAQ, and log viewer with error count badge.

Output: AppFooter.vue component and updated App.vue with footer integration and temporary mechanisms removed.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-app-footer-integration/12-RESEARCH.md
@.planning/phases/12-app-footer-integration/12-01-SUMMARY.md

@src/renderer/src/App.vue
@src/renderer/src/stores/logStore.ts
@src/renderer/src/types/log.ts
@src/shared/types/api.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AppFooter component</name>
  <files>src/renderer/src/components/AppFooter.vue</files>
  <action>
    Create `src/renderer/src/components/AppFooter.vue` using Vue 3 Composition API with `<script setup lang="ts">`.

    **Template structure:**
    - `<v-footer app color="#E5AA94" class="px-4 py-1" height="auto">` -- use `app` prop for Vuetify layout coordination, `py-1` for compact height
    - Outer flex container: `<div class="d-flex align-center justify-space-between" style="width: 100%">`

    **Left section -- Version menu:**
    - `<v-menu>` with `<template v-slot:activator="{ props }">` pattern
    - Activator: `<v-btn v-bind="props" variant="text" size="small" class="text-caption">` showing `v{{ appVersion }}`
    - Menu content: `<v-list density="compact">` with one `<v-list-item>` showing:
      - `<v-list-item-title>VarLens v{{ appVersion }}</v-list-item-title>`
      - `<v-list-item-subtitle>Electron v{{ electronVersion }}</v-list-item-subtitle>`

    **Right section -- Action buttons (inside `<div class="d-flex align-center ga-1">`):**
    1. GitHub: `<v-btn icon="mdi-github" size="small" variant="text" aria-label="Open GitHub repository" @click="openGitHub" />`
    2. License: `<v-btn icon="mdi-license" size="small" variant="text" aria-label="View license" @click="openLicense" />`
    3. Disclaimer: `<v-btn :icon="disclaimerAcknowledged ? 'mdi-shield-check' : 'mdi-shield-alert'" :color="disclaimerAcknowledged ? 'success' : 'warning'" size="small" variant="text" aria-label="View disclaimer" @click="openDisclaimer" />`
    4. FAQ: `<v-btn icon="mdi-help-circle" size="small" variant="text" aria-label="Open FAQ" @click="openFAQ" />`
    5. Log viewer with badge: `<v-badge :content="errorCount" :model-value="errorCount > 0" color="error" overlap>` wrapping `<v-btn icon="mdi-console" size="small" variant="text" aria-label="Toggle log viewer" @click="toggleLogViewer" />`

    **Script section:**
    - Import `ref`, `computed`, `onMounted` from vue
    - Import `storeToRefs` from pinia
    - Import `useLogStore` from `../stores/logStore`
    - Add `/* global window */` comment for ESLint

    **Props:**
    - `disclaimerAcknowledged: { type: Boolean, default: true }` -- receives disclaimer state from parent (Phase 11 will provide the real value; default true so shield-check shows when Phase 11 not yet implemented)

    **Emits:**
    - `'toggle-log-viewer'` -- tell App.vue to toggle LogViewer drawer
    - `'open-disclaimer'` -- tell App.vue to open disclaimer dialog
    - `'open-faq'` -- tell App.vue to open FAQ dialog

    **State:**
    - `appVersion = ref('...')` -- placeholder until onMounted fetches
    - `electronVersion = ref('')`

    **Log store integration:**
    - `const logStore = useLogStore()`
    - `const { stats } = storeToRefs(logStore)`
    - `const errorCount = computed(() => stats.value.errorCount + stats.value.criticalCount)`

    **Lifecycle:**
    - `onMounted(async () => { ... })` -- call `window.api.system.getVersion()` and set `appVersion.value` and `electronVersion.value`. Wrap in try/catch. Guard with `typeof window.api !== 'undefined'` check (for test environment safety, matching existing pattern in CaseList.vue etc.).

    **Handlers:**
    - `toggleLogViewer()`: emit 'toggle-log-viewer'
    - `openDisclaimer()`: emit 'open-disclaimer'
    - `openFAQ()`: emit 'open-faq'
    - `async openGitHub()`: call `window.api.shell.openExternal('https://github.com/berntpopp/varlens')`, log error if `result.success` is not true. Guard with `typeof window.api !== 'undefined'`.
    - `async openLicense()`: call `window.api.shell.openExternal('https://opensource.org/licenses/MIT')`, same error handling.

    **Lint compliance notes:**
    - Use `=== true` / `=== false` for strict boolean checks (ESLint strict-boolean-expressions)
    - Use `?? ''` instead of `|| ''` for nullish values
    - Ensure proper attribute ordering per vue/attributes-order
    - Use explicit type annotations where possible
  </action>
  <verify>
    - `npx tsc --noEmit` passes
    - `npm run lint:check` passes (or only pre-existing warnings)
    - File exists at `src/renderer/src/components/AppFooter.vue`
    - Component has v-footer with app prop and color="#E5AA94"
    - All 5 icon buttons present with aria-labels
    - v-badge wraps log viewer button with reactive errorCount
    - v-menu with activator slot for version popup
  </verify>
  <done>
    AppFooter.vue renders a persistent footer bar with version menu (left), and GitHub, license, disclaimer, FAQ, and log viewer buttons (right). Error count badge updates reactively from logStore. Version fetched via IPC on mount.
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate AppFooter into App.vue and remove temporary mechanisms</name>
  <files>src/renderer/src/App.vue</files>
  <action>
    Update `src/renderer/src/App.vue`:

    **Template changes:**
    1. ADD `<AppFooter>` component AFTER `</v-main>` and BEFORE `<ImportDialog>`:
       ```
       <AppFooter
         :disclaimer-acknowledged="disclaimerAcknowledged"
         @toggle-log-viewer="logViewerOpen = logViewerOpen === false"
         @open-disclaimer="handleOpenDisclaimer"
         @open-faq="handleOpenFAQ"
       />
       ```
    2. REMOVE the entire temporary FAB button block (lines ~49-60 in current App.vue):
       ```
       <!-- Floating action button for log viewer (temporary until footer exists) -->
       <v-btn icon="mdi-console" ... class="log-viewer-fab" ... />
       ```
    3. Keep the `<LogViewer v-model:open="logViewerOpen" />` as-is (footer emits toggle, App.vue controls state)

    **Script changes:**
    1. ADD import: `import AppFooter from './components/AppFooter.vue'`
    2. ADD disclaimer state placeholder: `const disclaimerAcknowledged = ref(true)` -- Phase 11 will wire this to the real disclaimer store. Default true so footer shows green shield.
    3. ADD handler stubs:
       - `const handleOpenDisclaimer = (): void => { logService.info('Disclaimer dialog requested', 'Footer') }` -- placeholder until Phase 11 components exist
       - `const handleOpenFAQ = (): void => { logService.info('FAQ dialog requested', 'Footer') }` -- placeholder until Phase 11 components exist
    4. REMOVE demo log seeding block from `onMounted` (the `window.setTimeout(() => { logService.info('Application started'...` block). The demo logs were for Phase 10 verification only; they are no longer needed now that the logging pipeline is verified.
    5. KEEP the `handleKeydown` for Ctrl+L -- keyboard shortcut is a permanent power-user feature (per Phase 11 context: "Shortcuts are kept permanently as secondary/power-user access").

    **Important: Do NOT modify any other existing functionality** (sidebar, case management, filters, variant table, import dialog, snackbar).
  </action>
  <verify>
    - `npm run dev` starts without errors
    - `npm run lint:check` passes
    - `npx tsc --noEmit` passes
    - Footer bar is visible at bottom of app with warm background
    - No temporary FAB button visible
    - Ctrl+L still toggles log viewer
    - Version popup shows app and Electron versions
    - GitHub button triggers external link (may need Electron environment to fully test)
  </verify>
  <done>
    App.vue integrates AppFooter with all event wiring. Temporary FAB removed. Demo log seeding removed. Footer visible at bottom with all 6 elements (version menu + 5 icon buttons). Disclaimer/FAQ handlers are placeholder stubs logging via LogService until Phase 11 provides real dialogs.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Persistent footer bar integrated into the app with:
    - Version popup menu (left side) showing app and Electron versions
    - GitHub, license, disclaimer, FAQ, and log viewer icon buttons (right side)
    - Error count badge on log viewer button (reactive from logStore)
    - Disclaimer status indicator (green shield-check by default, will show warning when Phase 11 is integrated)
    - Temporary FAB button removed
    - All buttons have ARIA labels for accessibility
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` in the project root
    2. Verify footer bar appears at bottom with warm #E5AA94 background color
    3. Click the version text (left side) -- popup should show VarLens version and Electron version
    4. Click the GitHub icon button -- should open https://github.com/berntpopp/varlens in system browser
    5. Click the license icon button -- should open https://opensource.org/licenses/MIT in system browser
    6. Verify the disclaimer button shows a green shield-check icon (acknowledged state)
    7. Click the FAQ button -- check browser console for "FAQ dialog requested" log message
    8. Click the log viewer button (console icon) -- LogViewer drawer should open/close
    9. Verify the error badge only appears when there are error/critical log entries (initially hidden since count is 0)
    10. Press Ctrl+L -- log viewer should still toggle via keyboard shortcut
    11. Verify no floating FAB button appears anywhere on screen
    12. Verify footer does not overlap or interfere with main content area
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. Footer bar visible with #E5AA94 background at bottom of every view
2. Version popup menu displays app version and Electron version
3. GitHub and license buttons open correct URLs in system browser
4. Disclaimer icon shows appropriate visual state
5. FAQ button emits event (placeholder handler logs to LogService)
6. Log viewer toggle button works, error badge updates reactively
7. No temporary FAB button visible
8. Ctrl+L keyboard shortcut still works
9. Footer does not overlap content or cause layout issues
10. `npm run lint:check` and `npx tsc --noEmit` pass
</verification>

<success_criteria>
- Persistent footer bar with #E5AA94 warm background visible at bottom of every view
- Version popup menu shows current app version and Electron version
- GitHub icon opens https://github.com/berntpopp/varlens in system browser
- License icon opens https://opensource.org/licenses/MIT in system browser
- Disclaimer status indicator shows green shield-check (default acknowledged state)
- FAQ button triggers handler (placeholder until Phase 11)
- Log viewer toggle with reactive error count badge (errors + critical from logStore)
- Temporary FAB button removed from App.vue
- Demo log seeding removed from App.vue
- All icon buttons have ARIA labels
- No layout conflicts between footer and main content
</success_criteria>

<output>
After completion, create `.planning/phases/12-app-footer-integration/12-02-SUMMARY.md`
</output>
