---
phase: 13-sqlcipher-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - electron.vite.config.ts
  - Makefile
  - src/main/database/DatabaseService.ts
  - src/main/database/schema.ts
  - src/main/database/index.ts
  - src/main/index.ts
  - tests/main/database/schema.test.ts
  - .github/workflows/build.yml
  - .github/workflows/release.yml
autonomous: true

must_haves:
  truths:
    - "All existing tests pass unchanged with the new database library"
    - "The app builds and starts identically to v0.2.0 behavior"
    - "CI pipeline works on all three platforms (Windows, macOS, Linux)"
    - "No references to 'better-sqlite3' remain in source code or config (except @types devDependency)"
  artifacts:
    - path: "package.json"
      provides: "better-sqlite3-multiple-ciphers dependency and updated build scripts"
      contains: "better-sqlite3-multiple-ciphers"
    - path: "electron.vite.config.ts"
      provides: "Native module externalization for new library"
      contains: "better-sqlite3-multiple-ciphers"
    - path: "src/main/database/DatabaseService.ts"
      provides: "Database service using new library import"
      contains: "better-sqlite3-multiple-ciphers"
    - path: "src/main/database/schema.ts"
      provides: "Schema initialization using new library type"
      contains: "better-sqlite3-multiple-ciphers"
    - path: "src/main/index.ts"
      provides: "Main process startup verification using new library"
      contains: "better-sqlite3-multiple-ciphers"
  key_links:
    - from: "package.json"
      to: "electron.vite.config.ts"
      via: "externalized dependency name must match installed package"
      pattern: "better-sqlite3-multiple-ciphers"
    - from: "package.json"
      to: ".github/workflows/build.yml"
      via: "rebuild scripts reference correct module name"
      pattern: "rebuild:node|rebuild:electron"
    - from: "src/main/database/DatabaseService.ts"
      to: "package.json"
      via: "import resolves to installed package"
      pattern: "from 'better-sqlite3-multiple-ciphers'"
---

<objective>
Swap the native database module from `better-sqlite3` to `better-sqlite3-multiple-ciphers` across the entire codebase -- source files, test files, build pipeline, CI workflows, and packaging config -- while preserving identical application behavior.

Purpose: This is the foundation for database encryption in v0.3.0. The library swap must be invisible: same API, same behavior, same test results. The encryption capability remains dormant until Phase 14.

Output: All imports, scripts, and configurations reference `better-sqlite3-multiple-ciphers`. All existing tests pass. Build pipeline produces working packages.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-sqlcipher-foundation/13-CONTEXT.md
@.planning/phases/13-sqlcipher-foundation/13-RESEARCH.md
@package.json
@electron.vite.config.ts
@Makefile
@src/main/database/DatabaseService.ts
@src/main/database/schema.ts
@src/main/database/index.ts
@src/main/index.ts
@tests/main/database/schema.test.ts
@.github/workflows/build.yml
@.github/workflows/release.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap npm dependency and update all build/packaging config</name>
  <files>
    package.json
    electron.vite.config.ts
    .github/workflows/build.yml
    .github/workflows/release.yml
  </files>
  <action>
    1. In `package.json`:
       - Remove `"better-sqlite3"` from `dependencies`
       - Add `"better-sqlite3-multiple-ciphers": "^12.6.2"` to `dependencies`
       - Keep `"@types/better-sqlite3": "^7.6.13"` in `devDependencies` (the fork uses the same TypeScript types from DefinitelyTyped -- the types package is compatible)
       - Update `scripts.postinstall` from `npx @electron/rebuild -f -w better-sqlite3` to `npx @electron/rebuild -f -w better-sqlite3-multiple-ciphers`
       - Update `scripts["rebuild:electron"]` from `npx @electron/rebuild -f -w better-sqlite3` to `npx @electron/rebuild -f -w better-sqlite3-multiple-ciphers`
       - Update `scripts["rebuild:node"]` from `npm rebuild better-sqlite3` to `npm rebuild better-sqlite3-multiple-ciphers`
       - Update `build.asarUnpack` from `["node_modules/better-sqlite3/**/*"]` to `["node_modules/better-sqlite3-multiple-ciphers/**/*"]`
       - Update `build.files` from including `"node_modules/better-sqlite3/**/*"` to `"node_modules/better-sqlite3-multiple-ciphers/**/*"`

    2. In `electron.vite.config.ts`:
       - Change `external: ['better-sqlite3']` to `external: ['better-sqlite3-multiple-ciphers']` in the main build rollupOptions

    3. In `.github/workflows/build.yml` and `.github/workflows/release.yml`:
       - No script name changes needed (the npm scripts are already referenced by name: `npm run rebuild:node`, `npm run rebuild:electron`)
       - Verify the Linux system dependencies step -- `libsqlite3-dev` is NOT needed for better-sqlite3-multiple-ciphers as it bundles its own SQLite source. However, keep it for safety (doesn't hurt). No changes to CI workflows unless the module name appears directly (it does not -- only npm script names are used).

    4. Run `npm install` to install the new dependency and remove the old one. This will also trigger the postinstall rebuild for Electron.

    5. Run `npm run rebuild:node` to rebuild for Node.js (needed for tests).

    WARNING: Do NOT remove `@types/better-sqlite3` from devDependencies. The TypeScript types are provided by this separate package and better-sqlite3-multiple-ciphers uses the same type definitions. The import `from 'better-sqlite3-multiple-ciphers'` resolves types via TypeScript path mapping or declaration merging -- if types don't resolve, add a `declare module 'better-sqlite3-multiple-ciphers'` shim in a `.d.ts` file that re-exports from `@types/better-sqlite3`. Check if the library ships its own types first.
  </action>
  <verify>
    - `cat package.json | grep -c "better-sqlite3-multiple-ciphers"` returns 4+ (dependency, postinstall, rebuild:electron, rebuild:node or asarUnpack/files)
    - `cat package.json | grep "\"better-sqlite3\"" | grep -v "multiple-ciphers" | grep -v "@types"` returns nothing (old dep fully removed)
    - `cat electron.vite.config.ts | grep "better-sqlite3-multiple-ciphers"` returns match
    - `ls node_modules/better-sqlite3-multiple-ciphers/package.json` succeeds
    - `ls node_modules/better-sqlite3/package.json 2>/dev/null` fails (old package removed)
  </verify>
  <done>
    package.json has better-sqlite3-multiple-ciphers as dependency with all scripts and build config updated. electron.vite.config.ts externalizes the new module. Old better-sqlite3 is fully removed from dependencies. npm install and rebuild:node complete without errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update all source and test imports, verify tests pass</name>
  <files>
    src/main/database/DatabaseService.ts
    src/main/database/schema.ts
    src/main/index.ts
    tests/main/database/schema.test.ts
  </files>
  <action>
    1. In `src/main/database/DatabaseService.ts` (line 8-9):
       - Change `import Database from 'better-sqlite3'` to `import Database from 'better-sqlite3-multiple-ciphers'`
       - Change `import type { Database as DatabaseType, Statement } from 'better-sqlite3'` to `import type { Database as DatabaseType, Statement } from 'better-sqlite3-multiple-ciphers'`
       - Update the JSDoc comment on line 5 from "Uses better-sqlite3" to "Uses better-sqlite3-multiple-ciphers"

    2. In `src/main/database/schema.ts` (line 7):
       - Change `import type Database from 'better-sqlite3'` to `import type Database from 'better-sqlite3-multiple-ciphers'`
       - Update JSDoc on line 141 referencing "better-sqlite3 Database instance" to "better-sqlite3-multiple-ciphers Database instance"

    3. In `src/main/index.ts` (line 4):
       - Change `import Database from 'better-sqlite3'` to `import Database from 'better-sqlite3-multiple-ciphers'`
       - Update the console.log on line 92 from 'better-sqlite3 initialized successfully' to 'better-sqlite3-multiple-ciphers initialized successfully'
       - Update the error messages on lines 97 and 103 to reference 'better-sqlite3-multiple-ciphers' instead of 'better-sqlite3'
       - Update the comment on line 87 similarly

    4. In `tests/main/database/schema.test.ts` (line 2):
       - Change `import Database from 'better-sqlite3'` to `import Database from 'better-sqlite3-multiple-ciphers'`

    5. In `src/main/database/index.ts`:
       - No import changes needed (it imports from local `./DatabaseService`, not from better-sqlite3 directly)

    6. In `Makefile`:
       - No changes needed (it references npm script names, not the module directly)

    NOTE on TypeScript type resolution: `better-sqlite3-multiple-ciphers` may or may not ship its own TypeScript types. If `import Database from 'better-sqlite3-multiple-ciphers'` causes TypeScript errors after the swap:
       - Check if the package has a `types` field in its package.json or a bundled `.d.ts`
       - If not, create `src/main/database/better-sqlite3-multiple-ciphers.d.ts` with content:
         ```typescript
         declare module 'better-sqlite3-multiple-ciphers' {
           export * from 'better-sqlite3'
           export { default } from 'better-sqlite3'
         }
         ```
       This re-exports all types from the existing `@types/better-sqlite3` package.

    7. Run `npm run typecheck` to verify TypeScript compilation succeeds.
    8. Run `npm run lint:check` to verify no lint errors.
    9. Run `npm run test` to verify ALL existing tests pass unchanged.
  </action>
  <verify>
    - `grep -r "from 'better-sqlite3'" src/ tests/ --include="*.ts" | grep -v "multiple-ciphers"` returns nothing (no old imports remain)
    - `npm run typecheck` exits 0
    - `npm run lint:check` exits 0
    - `npm run test` exits 0 with all tests passing
  </verify>
  <done>
    All source files and test files import from 'better-sqlite3-multiple-ciphers'. TypeScript compilation, linting, and all existing tests pass with zero failures. The library swap is invisible to the test suite.
  </done>
</task>

</tasks>

<verification>
1. `grep -r "better-sqlite3" src/ tests/ package.json electron.vite.config.ts Makefile .github/ --include="*.ts" --include="*.json" --include="*.yml" --include="*.mts" | grep -v "multiple-ciphers" | grep -v "@types" | grep -v "node_modules"` -- should return nothing (all references updated)
2. `npm run test` -- all existing tests pass
3. `npm run typecheck` -- TypeScript compilation succeeds
4. `npm run lint:check` -- no lint errors
5. `npm run build` -- electron-vite build succeeds (verifies externalization works)
</verification>

<success_criteria>
- Zero references to plain `better-sqlite3` remain in source/config (except @types devDep if still needed)
- All existing tests pass with the new library -- zero test modifications to logic, only import paths changed
- TypeScript compilation succeeds
- electron-vite build succeeds with the new externalized module
- The Makefile help comments may still reference "better-sqlite3" in description text -- that is acceptable
</success_criteria>

<output>
After completion, create `.planning/phases/13-sqlcipher-foundation/13-01-SUMMARY.md`
</output>
