# Milestone v0.1: POC

**Status:** ✅ SHIPPED 2026-01-27
**Phases:** 1-8
**Total Plans:** 17

## Overview

Varlens v0.1 POC validates the core Electron + Vue 3 + SQLite stack by delivering a working application that imports gzipped JSON variant data, displays variants in a paginated table, and provides basic filtering. The 8 phases build from foundation through full user experience, with each phase delivering verifiable functionality.

## Phases

### Phase 1: Foundation

**Goal**: Working Electron app scaffold with all tooling configured
**Depends on**: Nothing (first phase)
**Requirements**: FOUND-01, FOUND-02, FOUND-03, FOUND-04, FOUND-05, FOUND-06
**Plans**: 2 plans

Plans:
- [x] 01-01-PLAN.md — Scaffold project, configure Vuetify 3, integrate better-sqlite3
- [x] 01-02-PLAN.md — Configure ESLint, Vitest, and Makefile

**Details:**
- electron-vite@5.0.0 for build system
- Vue 3 + Vuetify 3 + TypeScript
- better-sqlite3 with native module rebuild
- ESLint 9 flat config with strict TypeScript rules
- Vitest with happy-dom and 70% coverage thresholds
- Makefile with dev/build/lint/test/typecheck commands

### Phase 2: Database Layer

**Goal**: SQLite database with schema, FTS5, and complete DatabaseService API
**Depends on**: Phase 1
**Requirements**: DB-01, DB-02, DB-03, DB-04, DB-05, DB-06, DB-07, DB-08
**Plans**: 3 plans

Plans:
- [x] 02-01-PLAN.md — Create types, error classes, and schema definitions with FTS5
- [x] 02-02-PLAN.md — Create DatabaseService with initialization and case CRUD
- [x] 02-03-PLAN.md — Add variant batch insert, pagination, filters, and FTS5 search

**Details:**
- SQLite schema with cases and variants tables
- FTS5 virtual table for gene_symbol search
- Cursor-based pagination for efficient large result sets
- Dynamic filter building (gene, consequence, gnomAD AF, CADD)
- Transaction wrapper for batch operations
- Prepared statement caching

### Phase 3: Import Service

**Goal**: Import gzipped JSON files into database with progress reporting
**Depends on**: Phase 2
**Requirements**: IMP-01, IMP-02, IMP-03, IMP-04, IMP-05
**Plans**: 2 plans

Plans:
- [x] 03-01-PLAN.md — Create import types, field mapping config, and FieldMapper Transform
- [x] 03-02-PLAN.md — Create ImportService with streaming pipeline and integration tests

**Details:**
- stream-json for memory-efficient JSON parsing
- Streaming pipeline: gunzip → parser → pick → streamArray → FieldMapper → BatchAccumulator
- 5000-variant batch inserts
- Progress callbacks with phase/count/elapsed
- Gene dictionary extraction from file header
- 65k variants imported in ~20 seconds

### Phase 4: IPC Layer

**Goal**: Type-safe IPC bridge connecting renderer to main process services
**Depends on**: Phase 3
**Requirements**: IPC-01, IPC-02, IPC-03, IPC-04, IPC-05, IPC-06, IPC-07
**Plans**: 3 plans

Plans:
- [x] 04-01-PLAN.md — Create shared IPC types and error handling infrastructure
- [x] 04-02-PLAN.md — Implement preload bridge and integrate IPC registration
- [x] 04-03-PLAN.md — Implement domain IPC handlers (cases, variants, import, system)

**Details:**
- Preload script with contextBridge API
- Namespaced API: window.api.cases/variants/import/system
- Type declarations for renderer API
- Error serialization across IPC boundary
- Progress event throttling (100ms)
- File dialog with filters

### Phase 5: UI Shell + Cases

**Goal**: App layout with case sidebar that lists, selects, and deletes cases
**Depends on**: Phase 4
**Requirements**: CASE-01, CASE-02, CASE-03
**Plans**: 2 plans

Plans:
- [x] 05-01-PLAN.md — App shell layout with collapsible sidebar and empty state
- [x] 05-02-PLAN.md — Case list with selection, context menu delete, and notifications

**Details:**
- Vuetify v-navigation-drawer with permanent mode
- Case list with v-list single selection
- Context menu positioned at mouse coordinates
- Delete confirmation dialog with promise-based API
- AppSnackbar for notifications

### Phase 6: Variant Table

**Goal**: Paginated variant table using Vuetify v-data-table-server with backend sorting
**Depends on**: Phase 5
**Requirements**: TBL-01, TBL-02, TBL-03, TBL-04, TBL-05
**Plans**: 2 plans

Plans:
- [x] 06-01-PLAN.md — Create VariantTable component with server-side pagination and formatted columns
- [x] 06-02-PLAN.md — Implement backend sorting support in DatabaseService and IPC layer

**Details:**
- v-data-table-server with server-side pagination
- Multi-column sorting with cursor cache invalidation
- Column formatting: thousand separators, scientific notation for AF
- ClinVar color chips (red=pathogenic, green=benign, amber=VUS)
- Allele truncation at 20 characters with tooltip

### Phase 7: Filters

**Goal**: Filter toolbar with gene, consequence, AF, CADD, and FTS5 search
**Depends on**: Phase 6
**Requirements**: FLT-01, FLT-02, FLT-03, FLT-04, FLT-05, FLT-06
**Plans**: 2 plans

Plans:
- [x] 07-01-PLAN.md — Create FilterToolbar component with gene autocomplete, consequence dropdown, AF/CADD presets
- [x] 07-02-PLAN.md — Integrate FilterToolbar with VariantTable and App.vue

**Details:**
- FTS5 gene autocomplete with BM25 ranking
- Consequence multi-select populated from database
- AF/CADD preset chips with custom input
- 300ms debounce for auto-apply
- Cursor cache invalidation on filter change
- Filters clear on case switch

### Phase 8: Import UI + Polish

**Goal**: Complete import dialog and end-to-end POC verification
**Depends on**: Phase 7
**Requirements**: UIMP-01, UIMP-02, UIMP-03, UIMP-04, UIMP-05
**Plans**: 2 plans

Plans:
- [x] 08-01-PLAN.md — Create ImportDialog component with progress, validation, and error handling
- [x] 08-02-PLAN.md — Integrate ImportDialog into App.vue with sidebar button and auto-selection

**Details:**
- Import dialog with case name input and file selector
- File filter for .json, .json.gz, .gz
- Real-time progress indicator
- Auto-populate case name from filename
- Auto-select newly imported case
- Success snackbar with formatted variant count

---

## Milestone Summary

**Key Decisions:**
- D001-D010: Foundation decisions (electron-vite, strict TS, Vuetify autoImport, ESLint 9, happy-dom)
- D011-D018: Database decisions (snake_case, FTS5 external content, batch size 5000, null handling)
- D019-D025: Import decisions (column indices, streaming JSON, dictionary extraction)
- D026-D035: IPC decisions (error codes, serialization, namespaced API, singleton service)
- D036-D041: UI Shell decisions (permanent drawer, context menu, defineExpose)
- D042-D050: Variant Table decisions (cursor pagination, dynamic slots, ClinVar colors)
- D051-D060: Filter decisions (debounce, FTS5 autocomplete, state management)
- D061-D066: Import UI decisions (indeterminate progress, auto-select, orchestration)

**Issues Resolved:**
- stream-json event structure understanding (separate fix commit)
- ESLint vue/valid-v-slot with dot-containing slot names
- TypeScript project references in typecheck
- Vuetify CSS handling in Vitest

**Technical Debt:**
- Performance test shows 32s (marginally over 30s target) — environmental variance
- System APIs (system:version, system:userDataPath) exposed but unused — reserved for future

---

*For current project status, see .planning/ROADMAP.md*

---
*Archived: 2026-01-27 as part of v0.1 milestone completion*
